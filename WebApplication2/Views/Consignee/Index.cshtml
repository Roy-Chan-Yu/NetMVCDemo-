@model IEnumerable<WebApplication2.Models.consignee_manageme>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.consignee_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.consignee_address)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.consignee_code)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.cinsignee_phone)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.user_info.user_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Memo)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.consignee_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.consignee_address)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.consignee_code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.cinsignee_phone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.user_info.user_name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Memo)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.Id })
            </td>
        </tr>
    }

</table>
